
import React, { useState } from "react";
import { BadgeCheck, AlertCircle, TrendingUp, Users, Link, CheckCircle, PieChart, HelpCircle, Sparkle, ArrowRightCircle } from "lucide-react";

const dataCompare = [
  {
    label: "Judgment Threshold",
    left: { icon: <AlertCircle className="text-pink-500" />, text: "Low, Emotion-Driven", hint:"Anyone can vote without barriers" },
    right: { icon: <BadgeCheck className="text-green-500" />, text: "High, Requires PT + Check", hint: "Must stake PT and pass research-based Check" },
  },
  {
    label: "Responsibility Mechanism",
    left: { icon: <AlertCircle className="text-pink-500" />, text: "No Responsibility", hint:"No consequences for erroneous decisions" },
    right: { icon: <CheckCircle className="text-green-500" />, text: "Check Failure Costs Responsibility", hint: "Failure incurs real clearance costs" },
  },
  {
    label: "Reward Allocation",
    left: { icon: <TrendingUp className="text-pink-500" />, text: "Token Airdrop", hint:"Short-term focus with arbitrary rewards" },
    right: { icon: <PieChart className="text-green-500" />, text: "Structured Bonus", hint: "Rewards linked to structural completion" },
  },
  {
    label: "Fan Relationship",
    left: { icon: <Users className="text-pink-500" />, text: "One-Way Following", hint:"Fans follow influencers blindly" },
    right: { icon: <Link className="text-green-500" />, text: "Two-Way Lock-In", hint:"Deep fan engagement via staking + subscription" },
  },
  {
    label: "Decision Logic",
    left: { icon: <Sparkle className="text-pink-500" />, text: "Emotion-Driven Likes", hint:"Superficial decision-making lacks validation" },
    right: { icon: <BadgeCheck className="text-green-500" />, text: "Mechanism + Data Validation", hint:"Each decision auditable and tracked" },
  },
  {
    label: "Industry Risks",
    left: { icon: <AlertCircle className="text-pink-500" />, text: "Community Overload", hint:"Prone to bubbles and unsustainable practices" },
    right: { icon: <CheckCircle className="text-green-500" />, text: "Accountability Governance", hint:"Prevents speculation and ensures stability" },
  },
  {
    label: "Earning Source",
    left: { icon: <TrendingUp className="text-pink-500" />, text: "Hype & Speculation", hint:"Profit from market frothiness and hype" },
    right: { icon: <PieChart className="text-green-500" />, text: "Research-Driven Returns", hint:"Gains tied to informed decisions" },
  }
];

const sparkWords = [
  "Consensus isn't a like, it's shared responsibility!",
  "Synbo: Research + Accountability Defeats Fake Consensus",
  "Real decisions need mechanisms beyond emotions",
  "Without Clearing Mechanisms, Governance Fails",
  "Earn rewards through actual contributions",
  "Shaping accountable consensus for Web3's future!"
];

const faqs = [
  {
    q: "Q: Why doesn't Synbo rely on voting?",
    a: "Synbo abandons emotion-driven likes and relies on mechanisms with responsibilities, clearing, and outcome-based consensus."
  },
  {
    q: "Q: What happens if a Broker misjudges?",
    a: "If a Check fails, the Broker faces clearance; only serious evaluations pass the chain and lead to benefits."
  },
  {
    q: "Q: What are the benefits for users?",
    a: "Participants' returns are directly linked to their structured project contributions and long-term completion efforts."
  },
  {
    q: "Q: How does Synbo uniquely improve governance?",
    a: "Governance is no longer speculative or hype-driven; it ensures auditable outcomes, shared responsibility, and fair distribution."
  }
];

const steps = [
  { icon: <BadgeCheck className="w-7 h-7 text-purple-400" />, label: "Step1", desc: "Stake PT and enter the consensus challenge" },
  { icon: <Sparkle className="w-7 h-7 text-cyan-400" />, label: "Step2", desc: "Participate in Check and submit research analysis" },
  { icon: <CheckCircle className="w-7 h-7 text-green-400" />, label: "Step3", desc: "Pass Check and earn structured bonuses" },
  { icon: <AlertCircle className="w-7 h-7 text-pink-400" />, label: "Step4", desc: "If Check fails, responsibility clearance occurs" },
];

function SparklingBanner() {
  const [idx, setIdx] = useState(0);
  React.useEffect(() => {
    const t = setInterval(() => setIdx((i) => (i + 1) % sparkWords.length), 2900);
    return () => clearInterval(t);
  }, []);
  return (
    <div className="mb-6 py-3 bg-gradient-to-r from-pink-100 via-yellow-100 to-cyan-100 dark:from-pink-900 dark:via-yellow-800 dark:to-cyan-900 rounded-2xl shadow ring-1 ring-pink-200 dark:ring-cyan-900 flex relative overflow-hidden animate-pulse border">
      <div className="w-full text-center text-xl md:text-2xl font-bold tracking-widest text-pink-600 dark:text-yellow-300">
        {sparkWords[idx]}
      </div>
    </div>
  );
}

function FancyLabel({ children }) {
  return (
    <span className="bg-gradient-to-r from-yellow-200 to-green-200 dark:from-yellow-700 dark:to-green-700 py-1 px-3 rounded-full font-bold text-base shadow-lg ring-1 ring-green-100 dark:ring-green-900">{children}</span>
  );
}

function CurveSVG() {
  return (
    <svg width="100%" height="80" viewBox="0 0 600 80" fill="none" className="block my-6">
      <defs>
        <linearGradient id="curve-gradient" x1="0" y1="0" x2="600" y2="0" gradientUnits="userSpaceOnUse">
          <stop stopColor="#e879f9"/>
          <stop offset="0.5" stopColor="#22d3ee"/>
          <stop offset="1" stopColor="#a7f3d0"/>
        </linearGradient>
      </defs>
      <path id="mainCurve"
        d="M 20 60 Q 150 10 300 40 Q 450 70 580 20"
        stroke="url(#curve-gradient)" strokeWidth="7" fill="none" opacity="0.8" />
      <circle cx="20" cy="60" r="9" fill="#e879f9" />
      <circle cx="150" cy="12" r="9" fill="#22d3ee" />
      <circle cx="300" cy="40" r="9" fill="#10b981" />
      <circle cx="450" cy="68" r="9" fill="#22d3ee" />
      <circle cx="580" cy="22" r="9" fill="#a7f3d0" />
    </svg>
  );
}

export default function SynboConsensusBulletinGlobal() {
  const [faqOpen, setFaqOpen] = useState(-1);
  return (
    <div className="min-h-screen bg-gradient-to-b from-purple-100 via-cyan-50 to-teal-50 dark:from-[#22223b] dark:via-[#202d3d] dark:to-[#26343f] flex flex-col px-2 pt-8 pb-16 gap-6 items-center font-sans">
      <SparklingBanner />
      <section className="glass-card2 px-12 py-8 shadow-lg w-full max-w-5xl">
        <h1 className="text-4xl md:text-6xl font-bold text-center gradient-text mb-6">
          One-Stop Guide: Synbo's "Consensus is NOT Voting"
        </h1>
        <p className="text-xl text-center">
          <strong>Synbo isn't DAO-style voting</strong>. It's a structural consensus powered by research, shared accountability, and real benefits.
        </p>
      </section>

      {/* Additional Eng Content Sections */}
    </div>
  );
}
